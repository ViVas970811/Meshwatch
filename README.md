# Meshwatch
Graph-native fraud detection in real time. Heterogeneous GNN (PyTorch Geometric) + XGBoost ensemble catches collusion rings tabular ML misses. Streams via Kafka, scores in &lt;50ms through FastAPI + Ray Serve, and auto-investigates alerts with a LangGraph agent. React dashboard visualizes the fraud network. Built on IEEE-CIS (590K transactions).
